package learning;
public class mathClass {
    public static void main(String[] args) {
        // System.out.println(Math.PI); // its means pie
        // System.out.println(Math.E); // it means e

        double result;

        result = Math.pow(2,3); // means 2^3 2 ki power 3
        result = Math.abs(-5); // retuens absolute value of the number 
        result = Math.sqrt(9); // square root.
        result = Math.round(3.14); // rounds to 3.0
        result = Math.ceil(3.14); // rounds upto cielling which is 4.0
        result = Math.floor(3.99); // rounds upto floor which is 3.0
        result = Math.max(10,20); // max = 20
        result = Math.min(10,20); // min = 10

        System.out.println(result);



    }
}
